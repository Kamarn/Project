<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Cart</title>
</head>
<body>
    
    {{> header }}

    {{#if ans}}
        <div class="container mt-3">
            <h2>Products in cart</h2>       
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Product name</th>
                        <th>Price</th>
                        <th>Selected quantity</th>
                    </tr>
                </thead>

                <tbody>
                    {{#each result}}
                        <tr>
                            <td>{{this.productName}}</td>
                            <td>{{this.productPrice}}</td>
                            <td>{{this.quantity}}</td>
                            <td class="text-end">
                                <a href="/cart/remove/{{this.productID}}" type="button" class="btn btn-light btn-small"><i class="bi bi-person-x"></i> Delete all</a>
                                <a href="/cart/removeOnlyOne/{{this.productID}}" type="button" class="btn btn-light btn-small"><i class="bi bi-person-x"></i> Delete one piece</a>
                            </td>
                        </tr>
                    {{/each}}
                </tbody>

            </table>
        </div>

        <center>
            <h3 >Total price {{totalPrice}} PLN</h3>
            <a href="/product/buy" class="btn btn-primary">Purchase</a>
        </center>  

    {{else}}
        <center>
            <h2 >There is no products in cart</h2> 
        </center>      
    {{/if}}

    {{#if messageError}}
        <h4 class="alert alert-danger mt-4">{{messageError}}</h4>
    {{/if}}

    {{#if messageSuccess}}
        <h4 class="alert alert-success mt-4">{{messageSuccess}}</h4>
    {{/if}}
</body>
</html>